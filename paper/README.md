# Paper

This directory contains the current manuscript and its compiled PDF.

Compile the paper from this directory:

```sh
latexmk -pdf -interaction=nonstopmode -halt-on-error \
  on-the-value-of-abstractions.tex
```
